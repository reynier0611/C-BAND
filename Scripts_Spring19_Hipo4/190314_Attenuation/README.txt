The code 'attenuation.cpp' is used to extract attenuation lengths.

As of now, we see attenuation values which are too big for what's expected.
Thus, other codes in this directory (such as 'diff_adc_tdc.cpp' are created to study this effect.
