This directory contains codes to check by hand the effect of different calibrations.
That is, if we have data for which calibrations have not yet been implemented, we can
run these codes and do the calibrations by hand.

Update the 'CCDB' tables from the 'input_params' directory.
